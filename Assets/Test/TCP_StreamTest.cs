using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;

public class TCP_StreamTest : MonoBehaviour {


    private float speed; 
    //private TCP_Stream dataInput;
    // Use this for initialization
    void Start () {
        //dataInput = this.gameObject.AddComponent<TCP_Stream>();

    }
	
	// Update is called once per frame
	void Update () {
       // speed = (float)Test_ReadData.speedForMono;
     //  Debug.Log("date time now: " + DateTime.Now);
        //Debug.Log("date time now ms: " + DateTime.Now.Millisecond);
        //Debug.Log("time.time " + Time.time);
         //Debug.Log("Speed: " + speed);
    }
}
